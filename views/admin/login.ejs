<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin Login</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
       <link rel="shortcut icon" href="assets/images/logo-sm.png">

  <style>
    body {
      margin: 0;
      padding: 0;
      background-image: url('https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTjB1Jv7gknwOLpok26SNZmEDjoHaQ-GFyC0Q&s');
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      height: 100vh;
      position: relative;
    }

    .overlay {
      position: absolute;
      inset: 0;
      background: rgba(0, 0, 0, 0.5);
      z-index: 0;
    }

    .login-wrapper {
      position: relative;
      z-index: 1;
    }

    .login-card {
      max-width: 400px;
      width: 100%;
      background-color: rgba(255, 255, 255, 0.9);
      border-radius: 8px;
      padding: 2rem;
    }
  </style>
</head>

<body>

  <div class="overlay"></div>

  <!-- Centering Container -->
  <div class="d-flex align-items-center justify-content-center min-vh-100 login-wrapper">
    <div class="card shadow login-card">
      <h2 class="text-center mb-4">Admin Login</h2>

      <form id="adminLoginForm" action="/admin/login" method="post">
        <div class="mb-3">
          <label for="adminEmail" class="form-label fw-medium">Email address</label>
          <input id="adminEmail" type="email" class="form-control" placeholder="admin@example.com" name="email" autocomplete="off" />
        </div>

        <div class="mb-3">
          <label for="adminPassword" class="form-label fw-medium">Password</label>
          <input id="adminPassword" type="password" class="form-control" placeholder="Your password" name="password" />
        </div>

        <% if (message) { %>
          <div class="alert alert-<%= messageType === 'success' ? 'success' : 'danger' %>" role="alert">
            <%= message %>
          </div>
          <% } %>

            <button type="submit" class="btn btn-primary w-100 mt-3">Sign In</button>
      </form>


    </div>
  </div>

</body>

</html>